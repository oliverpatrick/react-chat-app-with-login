import React, { ReactElement } from 'react';
import { FormPageContainer, FormPageLayout } from '../FormPageContainer';

export default function ForgottenPasswordPage(): ReactElement {
  return (
    <FormPageLayout>
      <FormPageContainer>
        <h2>Not implemented yet</h2>
      </FormPageContainer>
    </FormPageLayout>
  );
}
